# TASK {{TASK_ID}} - rubric (role: research standard designer)

Project: {{PROJECT_NAME}} | attempt {{ATTEMPT}}/{{MAX_ATTEMPTS}}
Bundle: `{{BUNDLE_PATH}}`

## Why
The engine must distinguish a novel load-bearing computation from a large
collage of known modules, and must distinguish novelty from theory depth. This
file contextualizes those tests for this project. It supplies no candidate
programs and does not reverse-engineer paper narratives.

## Do
Write `.evo/profile/INNOVATION_RUBRIC.md` with these exact sections:

- `## Scientific program` - identify this project's learned objects, update
  laws, inference rules, data/feedback flows and resource axes. Explain what a
  complete rebuild would be allowed to discard while preserving the external
  task/evaluation contract.
- `## Implementation scope` - define configuration, local, subsystem, and
  full-program replacement for this codebase. This is breadth/cost only; it
  never certifies novelty. State how an independent critic distinguishes a
  genuine full train+infer rebuild from a local kernel drawn on a large graph.
- `## Mechanism novelty` - define known, composition, irreducible, and paradigm
  kernels. State the project's emulation, non-reducibility, collage, and
  load-bearing removal tests. The emulation test must be operational: an
  alternative emulates the core only when its registered computation actually
  produces the same load-bearing intermediate, never when its function class
  could in principle express it. A one-file update law may be irreducible; a
  large multi-module rebuild may remain a known composition.
- `## Effect frontier` - name what constitutes a meaningful resource-matched
  gain on the configured C# cells, which resource deltas/confounds must be
  disclosed, and what counts as merely buying a better number.
- `## Theory axis` - distinguish no theory, explanatory theory, and a derivation
  that constrains or produces the executable program. Theory is independent of
  L level; state what implementation consequence and discriminating prediction
  make theory load-bearing here.
- `## Engineering boundary` - define what useful transfer, module composition,
  tuning and systems work belongs in engineering mode even when it gains.
- `## Project-specific tests` - concrete counterexamples and edge cases for this
  codebase: single-file innovation, full rebuild, subtraction, multi-component
  irreducible coupling, and superficial A+B stitching.

Each section must be substantive (>=160 chars). Use program shapes and
falsifiable tests, never named techniques, paper links or DOI/arXiv links.

## Output contract
{{OUTPUTS}}

## Submit
{{SUBMIT_CMD}}
